# Optional: enable GPT-4o mini for the COGNIGUIDE Assistant chatbot.
# Without this key, the assistant uses built-in context-aware responses from live patient data.
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here
